---
version: '3.2'

networks:
  infra_default:
    external: true
  st_network:
    external: true

services:
  # postgresql:
  #   image: postgres:9.6
  #   hostname: avanan-postgresql
  #   networks:
  #     st_network:
  #       aliases:
  #         - avanan-postgresql
  #   command: postgres -c max_connections=300
  #   environment:
  #     - POSTGRES_DB=avanan
  #     - POSTGRES_USER=avanan
  #     - POSTGRES_PASSWORD=avanan
  #   restart: unless-stopped
  #   ports:
  #     - 5432:5432

  app:
    build:
      context: .
    command: ./manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/usr/src/app
      - ./media_root:/usr/src/media_root
      - ./static_root:/usr/src/static_root
    environment:
      # - DB_NAME=avanan
      # - DB_USER=avanan
      # - DB_HOST=avanan-postgresql
      # - DB_PASSWORD=avanan
      # - DB_PORT=5432
      - DEBUG='true'
    restart: unless-stopped
    hostname: avanan-app
    networks:
      infra_default:
        aliases:
          - avanan-app
      st_network:
        aliases:
          - avanan-app
    ports:
      - 8000:8000
    # depends_on:
    #   - postgresql
